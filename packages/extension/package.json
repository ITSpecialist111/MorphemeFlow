{
  "name": "@morphemeflow/extension",
  "version": "0.1.0",
  "description": "MorphemeFlow browser extension — morpheme highlighting and adaptive typography for dyslexia reading support",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.mjs --watch",
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@morphemeflow/engine": "*"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0"
  },
  "license": "MIT"
}
